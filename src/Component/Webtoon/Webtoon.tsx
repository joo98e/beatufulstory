import React from 'react'
import styled from 'styled-components'
import { Section, SectionTitle } from '../../Assets/UIComponent'

interface Props {

}

const Webtoon: React.FC = (props: Props) => {
    return (
        <Section>
            <SectionTitle>sdfsdf</SectionTitle>
        </Section>
    )
}

export default Webtoon
